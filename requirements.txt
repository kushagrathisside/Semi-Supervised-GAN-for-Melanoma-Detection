# Pinned to the actual working environment (Python 3.12).
#
# IMPORTANT — install PyTorch first, matched to your CUDA, e.g. CUDA 12.8:
#   pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu128
# then: pip install -r requirements.txt

# Core Deep Learning
torch==2.10.0
torchvision==0.25.0

# Data Processing
numpy==2.4.3
pandas==3.0.3          # HAM10000 metadata parsing (scripts/prep_ham10000.py)
opencv-python==4.13.0.92
pillow==12.1.1
scikit-learn==1.8.0    # classification metrics (AUC, F1, confusion matrix)
scipy==1.17.1          # significance analysis (bootstrap / DeLong)

# Foundation model (DINOv2 hub loader requires these)
torchmetrics==1.9.0
termcolor==3.3.0

# Visualization & Monitoring
matplotlib==3.10.8
tensorboard==2.20.0
tqdm==4.67.3

# Configuration & Utilities
pyyaml==6.0.3
pydantic==2.13.4

# Evaluation Metrics
pytorch-fid==0.3.0

# Testing
pytest==9.0.3
pytest-cov==7.1.0
